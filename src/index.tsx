import * as React from 'react';
import * as ReactDOM from 'react-dom';

const Index = () => {
  return <div>Hello React with Typescript and hot reloading!</div>;
};

ReactDOM.render(<Index />, document.getElementById('app'));
